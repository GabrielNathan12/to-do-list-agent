apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-service
spec:
  type: {{ .Values.frontend.service }}
  selector:
    app: {{ .Values.frontend.name }}
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080
